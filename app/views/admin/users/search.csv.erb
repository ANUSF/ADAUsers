Username,Password,Role,Email,Institution,Type of work,Position,Date registered,Confirmed as ACSPRI member?,Title,First name,Surname
<%= CSV.generate do |csv|
  @users.each do |user|
    csv << [ user.user, user.password, user_roles_to_s(user.user_roles), user.email, user.institution, user.action, user.position, user.dateregistered, confirmed_acspri_member_to_s(user.confirmed_acspri_member), user.title, user.fname, user.sname ]
  end
end %>
